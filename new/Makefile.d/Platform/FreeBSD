# vim:ft=make:

# FreeBSD install some libraries into /usr/local instead of into /usr.
PLATFORM_LIBS=\
    -L /usr/local/lib \
	-lmd \
	-luuid

PLATFORM_INCLUDE_FLAGS=\
	-I /usr/local/include

install=ginstall
python3=python3.12
